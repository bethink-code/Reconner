atching Algorithm Bug Report
Summary
Two transactions with perfect matching conditions (same day, exact amount, unique candidates) are NOT matching.

Bug Cases
Case 1: R332.70
FieldBankFuelDate2025-09-142025-09-14AmountR332.70R332.70Payment TypeStandalone (Card)CardReference672150921379604Match StatusunmatchedunmatchedUnique?Yes - only 1 fuel candidate
Case 2: R506.90
FieldBankFuelDate2025-09-142025-09-14AmountR506.90R506.90Payment TypeStandalone (Card)CardReference672150921379465Match StatusunmatchedunmatchedUnique?Yes - only 1 fuel candidate

What SHOULD Happen
Based on the algorithm in routes.ts:

Amount check (line 951): amountDiff = 0.00 → PASSES (within tolerance)
Date check (line 969): dateDiff = 0 → PASSES (same day, within -1 to +3 window)
Base confidence: 85 (same day)
Card adjustment (line 1043-1046): Bank has card, fuel doesn't → -5 penalty
Final confidence: 80 → PASSES minimum threshold (70)

Expected: Match created with 80% confidence
Actual: No match created

Working Comparisons
On the same day (Sep 14), these DID match:
Bank AmountFuel AmountDiffMatched?R720.00R720.40R0.40✅ YesR1337.80R1337.80R0.00✅ YesR332.70R332.70R0.00❌ NoR506.90R506.90R0.00❌ No

Debug Suggestions
1. Add logging for these specific cases:
typescript// In the auto-match loop, before "for (const invoice of fuelInvoices)"
if (bankTx.amount === '332.7' || parseFloat(bankTx.amount) === 332.7) {
  console.log('DEBUG R332.70 bank tx:', bankTx.id);
  console.log('  Looking for fuel invoices...');
}

// Inside the invoice loop
if ((bankTx.amount === '332.7' || parseFloat(bankTx.amount) === 332.7) && 
    Math.abs(invoice.totalAmount - 332.7) < 1) {
  console.log('  Found candidate invoice:', invoice.invoiceNumber);
  console.log('  Amount:', invoice.totalAmount);
  console.log('  Date:', invoice.firstDate);
  console.log('  Already matched?', matchedInvoices.has(invoice.invoiceNumber));
  console.log('  Items matched?', invoice.items.some(item => item.matchStatus === 'matched'));
}
2. Check these potential issues:

Line 941: if (matchedInvoices.has(invoice.invoiceNumber)) continue;

Is invoice 1379604 somehow in matchedInvoices already?


Line 942: if (invoice.items.some(item => item.matchStatus === 'matched')) continue;

Is the item's matchStatus already 'matched' from a previous run?


Line 960-963: Date parsing

Is parseDateToDays() returning null for these dates?


Amount parsing:

Is parseFloat(bankTx.amount) returning NaN or wrong value for "332.7"?



3. Check for stale data:
If auto-match was run multiple times, there might be stale state:
sql-- Check if these transactions have matchId set but matchStatus is 'unmatched'
SELECT id, amount, match_status, match_id 
FROM transactions 
WHERE amount IN ('332.7', '506.9');

Other Issues Found
Issue 2: Tolerance Too Tight
Bank R141.00 doesn't match Fuel R140.05 (Sep 12)

Difference: R0.95
Current tolerance: Likely R0.10
Fix: Increase default tolerance to R0.50 or R1.00

Issue 3: 31 Unmatched Breakdown
CategoryCountNotesUnique match (BUG)4Should match - algorithm bugTime-solvable6Same-day narrowing would helpAmbiguous9Multiple same-day matches, need human reviewNo fuel exists12Genuine discrepancies

Multi-Item Invoice Note
The system correctly handles multi-item invoices (fuel + oil + cigarettes):

Invoice 1394315: 2 items (R100.10 + R100.20) = R200.30 → matched to R200.00 bank ✓

Invoice grouping is working correctly.